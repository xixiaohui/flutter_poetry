# 架构.md

# Flutter Poetry 系统架构

## 一、架构目标

本项目采用 BFF（Backend For Frontend）架构。

Flutter App **不能直接访问 Chinese Poetry API**。

所有客户端请求必须经过 Poetry Gateway。

Poetry Gateway 负责聚合数据、统一接口、缓存、安全、AI 能力以及未来业务扩展。

整个项目各模块职责必须严格分离。

---

# 二、整体架构

```text
                    Flutter App
                         │
                  HTTPS REST API
                         │
                 Poetry Gateway API
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
Chinese Poetry API   AI Service      User Service
        │                │                │
     SQLite         OpenAI/DeepSeek   PostgreSQL
```

---

# 三、模块职责

## Flutter App

职责：

- UI
- 页面导航
- 本地缓存
- 阅读体验
- 收藏（本地）
- 分享
- 动画
- Riverpod 状态管理

Flutter 永远不要知道：

- Chinese Poetry API
- SQLite
- AI Provider
- PostgreSQL

Flutter 只调用：

```

/api/v1/\*

```

---

## Poetry Gateway

Poetry Gateway 是整个系统唯一的业务入口。

所有 Flutter 请求必须经过 Gateway。

Gateway 负责：

- 聚合 Chinese Poetry API
- 数据转换
- REST API 输出
- AI 调用
- Redis 缓存（预留）
- 用户权限
- JWT
- 推荐算法
- 节气推荐
- 每日一句
- Banner
- 阅读统计
- 收藏同步
- 日志
- 限流
- 统一错误处理

Gateway 对 Flutter 提供统一的数据结构。

Flutter 不允许直接调用第三方 API。

---

## Chinese Poetry API

职责：

仅负责提供古诗词基础数据。

包括：

- 诗词
- 作者
- 朝代
- 分类
- 搜索
- GraphQL

禁止修改源码。

作为独立 Docker 服务运行。

如果以后更换诗词数据源，仅修改 Gateway。

Flutter 无需修改任何代码。

---

## AI Service

负责：

- AI 赏析
- AI 翻译
- AI 配图
- AI 问答
- AI 推荐

支持：

- OpenAI
- DeepSeek
- Claude
- Gemini

Gateway 调用 AI Service。

Flutter 不直接调用任何 AI Provider。

---

## User Service

负责：

- 登录
- JWT
- 用户信息
- 收藏同步
- 阅读历史
- 阅读统计
- 每日打卡
- 设置同步

数据库：

PostgreSQL

以后可以独立扩容。

---

# 四、调用流程

首页：

Flutter

↓

GET /api/v1/home

↓

Gateway

↓

Chinese Poetry API

↓

Gateway 聚合数据

↓

Flutter

---

AI赏析：

Flutter

↓

POST /api/v1/ai/analyse

↓

Gateway

↓

AI Service

↓

Gateway

↓

Flutter

---

收藏：

Flutter

↓

POST /api/v1/favorites

↓

Gateway

↓

User Service

↓

PostgreSQL

---

# 五、接口规范

Flutter 只能调用：

```

/api/v1/home
/api/v1/discover
/api/v1/search
/api/v1/poems
/api/v1/authors
/api/v1/categories
/api/v1/recommend
/api/v1/quote
/api/v1/ai/*
/api/v1/user/*
/api/v1/favorites/*
/api/v1/history/*
/api/v1/config

```

禁止：

```

Flutter
↓

Chinese Poetry API

```

禁止：

```

Flutter
↓

OpenAI

```

禁止：

```

Flutter
↓

PostgreSQL

```

---

# 六、Repository 规范

Flutter：

```

UI

↓

Riverpod

↓

Repository

↓

Dio

↓

Poetry Gateway

```

禁止：

```

UI

↓

Dio

```

---

# 七、Docker 部署

建议每个服务独立运行：

```

docker-compose

├── poetry-gateway
├── chinese-poetry-api
├── postgres
├── redis（预留）
├── nginx

```

所有服务之间通过 Docker Network 通信。

外网仅开放：

- 80
- 443

其它端口禁止直接暴露。

---

# 八、未来扩展

Gateway 必须支持未来增加：

- AI 赏析
- AI 配图
- 多语言
- 推荐算法
- 节气
- 每日一句
- 用户系统
- 后台管理
- 数据统计

扩展时不得修改 Flutter 架构。

不得修改 Chinese Poetry API。

只允许扩展 Gateway。

---

# 九、Claude Code 开发原则

Claude Code 必须遵守：

1. 不得绕过 Poetry Gateway。
2. 不得让 Flutter 直接访问 Chinese Poetry API。
3. 不得在 Flutter 中编写业务逻辑。
4. 所有业务逻辑必须放在 Gateway。
5. Flutter 只负责展示。
6. Gateway 负责所有业务聚合。
7. 保持各模块低耦合、高内聚。
8. 新功能优先扩展 Gateway，不修改已有架构。